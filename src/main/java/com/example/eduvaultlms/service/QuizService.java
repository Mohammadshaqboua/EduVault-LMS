package com.example.eduvaultlms.service;

public class QuizService {
    /**
     * if (passMark < 0 || passMark > 100) {
     *     throw new IllegalArgumentException("passMark يجب أن يكون بين 0 و 100");
     * }
     * */
}
